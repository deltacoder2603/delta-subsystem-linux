[package]
name = "dsl"
version = "0.1.0"
edition = "2021"

[dependencies]
nix = { version = "0.30", features = ["sched", "mount", "fs", "process", "hostname"] }
libc = "0.2"
toml = "0.8"
serde = { version = "1.0", features = ["derive"] }
